import { ConverteTempoPipe } from './converte-tempo.pipe';

describe('ConverteTempoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverteTempoPipe();
    expect(pipe).toBeTruthy();
  });
});
